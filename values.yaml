# Default values for juice-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

#Values for Check Point Nano Agent in Nginx Ingress Controller
nanoToken: "35741c8e-a5fa-40b4-9c9a-b8dabde8956d-63f91f9b-d4a8-4bc0-b81a-e472d649f781"

image:
  nginxCtlCpRepo: "mnicholssync/nginx-ingress-ctl-cp:demo"
  cpRepo: "mnicholssync/nginx-ingress-ctl-cp:cp-agent-alpine"

nameOverride: "juice-shop"
fullnameOverride: "juice-shop-0.1.1"

podAnnotations: {}

podSecurityContext: {}

securityContext: {}

service:

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: juice-shop.checkpoint.com
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
