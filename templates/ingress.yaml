apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-configuration
spec:
  rules:
  - host: {{.Values.appURL}}
    http:
      paths:
      - path: /
        backend:
          serviceName: juice-shop
          servicePort: 8080
